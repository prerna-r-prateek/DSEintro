# DSEintro
Virtual Index Card for assignment 01

# To run the code on Windows locally using Python's built-in HTTP server:
### 1. Navigate to DSEintro directory in a terminal
### 2. Type `python3 -m http.server 8000`
### 3. View rendering on `localhost:8000` in the browser
